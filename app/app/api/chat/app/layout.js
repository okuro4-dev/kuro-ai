export const metadata = {
  title: "Kuro AI",
  description: "Assistant intelligent",
};

export default function RootLayout({ children }) {
  return (
    <html lang="fr">
      <body>{children}</body>
    </html>
  );
}
